# {Module Name}

## 📖 Overview
Brief description of what this module does and its role in the VCR pipeline.

## 🏗️ Architecture / Theory
Explanation of the underlying concepts (e.g., FPN for fusion, Smooth Modulation for losses).
Use diagrams or code blocks if helpful.

## 🔑 Key Components
List main classes or functions.

### `ClassName`
Description and key parameters.

## 💻 Usage Examples
Concrete code snippets showing how to use the module in isolation.

```python
from src.{module} import ClassName

# Example usage
obj = ClassName(param=value)
output = obj(input)
```

## ⚙️ Configuration
Explain relevant config parameters in `config.yaml` or `hyperparameters.yaml` related to this module.

```yaml
{module}:
  param1: "value"
```
